# Quota Grove V1 验收报告

日期：2026-08-27

## 结论

V1 已完成原生应用构建、真实额度读取、核心窗口交互、四主题边界、安装包、隐私静态检查和短时性能验收。最终应用已安装到 `~/Applications/Quota Grove.app` 并在本机运行。

## 构建与安装

| 项目 | 结果 |
|---|---|
| Swift Debug 构建 | 通过 |
| Swift Release 构建 | 通过 |
| 应用 Info.plist | `plutil` 通过 |
| ad-hoc 签名 | `codesign --verify --deep --strict` 通过 |
| 目标架构 | arm64 |
| 最低系统 | macOS 13 |
| 应用包体积 | 7.7 MB（含四张正式背景） |
| `LSUIElement` | true，不显示 Dock 图标 |
| 安装脚本 | 通过，已安装并启动 |
| 卸载脚本 | `zsh -n` 语法通过；未在本轮执行，以保留交付应用 |

## 真实数据

安装版执行 `--snapshot-json` 成功。最终验证快照：

```json
{
  "windowMinutes": 10080,
  "remainingPercent": 66,
  "usedPercent": 34,
  "resetsAt": "2026-09-03T03:10:12Z",
  "source": "codex-local-event",
  "planType": "prolite"
}
```

百分比在任务进行期间会继续变化，因此此处只记录验收时刻，不作为固定演示数据。

## 自动边界与解析测试

安装版执行 `--self-test`：`PASS 19 项`。

- 主题边界：50、49、20、19、3、2、1、0 全部通过。
- 秋天、末日、废土进度条均与森林绿不同。
- 10080 分钟窗口优先选择通过。
- `remaining = 100 - used` 计算通过。
- Unix 重置时间和套餐字段解析通过。
- 无关事件忽略通过。
- 大于 100 的异常百分比拒绝通过。
- 缺少 7 天窗口时显示最长实际窗口通过。

当前 Command Line Tools 环境不包含 XCTest/Swift Testing 运行库，因此使用应用内置的零依赖自测入口；测试随 Release 二进制一起交付，可在目标 Mac 复验。

## 视觉与交互

| 验收项 | 结果 |
|---|---|
| 收起渲染 | 200×80 pt；Retina 输出 400×160 px |
| 展开渲染 | 200×178 pt；Retina 输出 400×356 px |
| 四套正式背景 | 森林、秋天、末日、废土均已按 2.5:1 等比填充 |
| 缺图回退 | 四套程序化回退场景已保留 |
| 单击展开/收起 | 真实桌面 UI 通过 |
| 双击刷新不切换高度 | 真实桌面 UI 通过 |
| 右键菜单 | 刷新、登录启动、位置重置、隐私、退出均可见 |
| 可访问性 | 暴露标题、剩余百分比、状态、单/双击提示和拖动帮助 |
| 贴边收纳 | 代码路径完成；本轮 UI 自动化未能稳定合成原生窗口拖动，待人工拖到物理屏幕边缘复验 |
| 多显示器 | 位置约束和屏幕变化处理已实现；本轮无第二台显示器做断连实测 |

## 性能

最终安装版在正式森林图片已解码、20 秒额度刷新开启的状态下，使用 `top` 连续采样 11 次、间隔 2 秒：

- 平均 CPU：0.53%。
- 内存：15–16 MB。
- 线程：常态约 11–18。

达到 PRD 的空闲平均 CPU 低于 1%、常驻内存低于 80 MB 目标。

额度扫描已优化为：每次最多读取候选文件尾部 256 KiB，按修改时间找到第一个可信事件即停止，新会话通过目录变更触发重新发现。

## 隐私静态检查

- `Sources` 中未发现 `URLSession`、`NWConnection`、网络框架调用、遥测或分析 SDK。
- 未发现 `auth.json` 或 `.codex/auth` 读取代码。
- 链接库仅包含 Apple 系统框架与 Swift 系统运行库。
- 应用包中只包含可执行文件、Info.plist、产品图标和四张背景。

## 发布前仍需的长周期/硬件验收

以下项目不能用一次短时开发验收替代，发布公测前应补做：

- 连续运行 8 小时的崩溃与内存增长观察。
- 真实多显示器连接、断开、排列变化。
- 人工拖动到左右物理边缘、悬停滑出和离开延迟收纳。
- 在一台没有 Swift 开发环境的干净 Mac 上验证安装、登录启动和卸载。
