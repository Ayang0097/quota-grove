# Quota Grove 产品需求文档

> 独立实现版 · Version 1.0
> 日期：2026-08-27
> 产品负责人：Ayang
> 工作名称：Quota Grove（额度森林）

## 1. 文档目的

本文档用于指导一个全新的 macOS 桌面额度工具从零开发。它只定义产品目标、用户体验、视觉结果、数据边界和验收标准，不规定具体代码结构。

开发者应依据本文档独立设计工程架构和代码实现，不复制、改写或翻译任何既有同类项目的源代码、注释、脚本、README、类名、文件结构或 Git 历史。

## 2. 独立开发边界

### 2.1 可以使用的输入

- 本需求文档。
- 产品负责人提供的界面参考截图。
- 产品负责人拥有使用和分发权利的背景图片、图标及其他素材。
- Apple 官方开发文档与公开系统 API 文档。
- OpenAI 官方公开文档。
- 在用户本人设备上合法观察到的应用运行状态和本地数据输出。
- 通用编程知识、开源依赖及其许可范围内允许使用的内容。

### 2.2 不允许使用的输入

- 不读取或复制现有同类项目的源代码仓库。
- 不复制其 Swift 文件、类型名称、函数名称、注释或工程结构。
- 不复制其安装脚本、配置文件、README 或发布说明。
- 不以现有代码为基础进行逐行重写、语言转换或变量改名。
- 不删除第三方署名后继续使用第三方的实质性代码或素材。

### 2.3 开发留痕

- 使用一个全新的空目录和全新的 Git 仓库。
- 从第一份需求分析、架构草案和第一个源文件开始保留提交记录。
- 记录关键设计决策、数据源调查、测试结果和素材来源。
- AI 辅助开发时，保留主要提示词、人工选择、修改和验收记录。

## 3. 产品概述

Quota Grove 是一款原生 macOS 桌面悬浮工具，用极小的常驻卡片显示用户当前的 Codex 剩余额度和重置时间。

它不占用菜单栏，也不显示 Dock 图标。用户只需看一眼桌面卡片，就能判断当前额度是否充足。随着剩余额度下降，卡片背景会从绿色森林逐步切换为秋天、末日和灰白废墟，用环境变化传达额度紧张程度。

### 3.1 一句话定位

> 用一张会随额度变化的桌面风景卡片，让重度 Codex 用户随时看见剩余额度。

### 3.2 核心价值

- 无需进入设置或额度页面。
- 不打断当前工作。
- 通过数字、进度和环境视觉共同表达额度状态。
- 完全在本机运行，不上传用户数据。

## 4. 目标用户

### 4.1 核心用户

- 每天长时间使用 Codex 的开发者、内容创作者和 AI 从业者。
- 经常需要判断是否应该继续执行大任务的用户。
- 希望桌面信息简洁，不愿增加菜单栏图标的用户。

### 4.2 典型使用场景

1. 用户开始一天工作时，快速确认 7 天额度是否充足。
2. 用户准备执行大型任务前，查看剩余额度和距离重置时间。
3. 额度进入 20% 以下时，通过明显的环境变化获得预警。
4. 用户暂时不想看到完整卡片时，将其拖到屏幕侧边收纳。
5. 用户需要更多信息时，点击卡片展开详情。

## 5. 产品目标与非目标

### 5.1 V1 目标

- 稳定读取本机 Codex 当前可获得的额度信息。
- 默认突出展示 7 天额度。
- 提供一张可移动、可展开、可侧边收纳的桌面悬浮卡片。
- 根据剩余额度切换四种视觉主题。
- 支持登录后自动运行。
- 无网络上传、无遥测、无账号密码存储。

### 5.2 V1 非目标

- 不开发菜单栏版本。
- 不开发 Windows 或 Linux 版本。
- 不提供云端同步。
- 不管理、购买或重置 Codex 额度。
- 不模拟、绕过或修改 Codex 的额度限制。
- 不使用 Codex 或 OpenAI 官方图标作为产品主标志。
- Touch Bar 展示不纳入 V1，可作为后续独立功能评估。

## 6. 信息架构

### 6.1 收起卡片必须展示

- `7 天额度`标题。
- 产品自有的小图标，放在标题文字之后。
- 距离额度重置的简短时间，例如`6 天 19 小时后重置`。
- 大号剩余百分比，例如`74%`。
- 百分比下方的`剩余`标签。
- 横向额度进度条。

收起状态不展示套餐、数据更新时间、已用百分比等次级信息。

### 6.2 展开卡片展示

展开后保留顶部摘要，并在下方展示：

- 7 天额度：剩余百分比与已用百分比。
- 距离重置：精确到天和小时。
- 订阅计划：仅在本地数据能可靠获得时展示。
- 数据更新：显示相对时间，例如`7 秒前`。

无法可靠获得的数据使用`--`，不得猜测。

## 7. 视觉规范

### 7.1 整体方向

- 关键词：简约、高级、克制、自然、清晰。
- 卡片主体使用深色环境背景，文字以白色为主。
- 不使用大量装饰、复杂统计或仪表盘式布局。
- 背景是情绪载体，信息始终保持第一视觉优先级。

### 7.2 卡片尺寸

| 状态 | 宽度 | 高度 |
|---|---:|---:|
| 收起 | 200 pt | 80 pt |
| 展开 | 200 pt | 178 pt |

- 点击展开时只改变高度，宽度始终保持 200 pt。
- 默认放在屏幕右上区域，与可用屏幕边缘保持约 20 pt 安全距离。
- 多显示器场景下，卡片应保留在用户最后操作的显示器内。

### 7.3 外轮廓

- 连续圆角：18 pt。
- 边框宽度：1 pt。
- 边框主色：带绿色倾向的浅色，例如 `#7AC99E`。
- 边框不透明度：80%。
- 边缘在深色和浅色桌面背景上都应可辨识，不得出现明显锯齿或发虚。
- 阴影应柔和但不能代替边框；阴影不得让卡片轮廓显得模糊。

### 7.4 背景素材

- 收起卡片比例为 2.5:1，背景素材应直接按相同比例制作。
- 建议源素材尺寸不低于 2000 × 800 px。
- 背景使用等比填充，不拉伸、不变形。
- 主体景物避开左侧标题、右侧数字和底部进度条区域。
- 背景上叠加轻度可读性遮罩，保证白色文字清楚。

### 7.5 字体层级

- 使用 macOS 系统字体，确保中文渲染稳定。
- 标题：中等字重，约 13–14 pt。
- 重置时间：常规或中等字重，约 10–11 pt，白色 55%–65%。
- 百分比：粗体，约 24–26 pt。
- `剩余`标签：约 9–10 pt，白色 55%–65%。
- 所有文字必须在 Retina 与非 Retina 缩放下保持清晰。

### 7.6 进度条

- 横向进度条位于卡片底部。
- 轨道使用深色半透明背景。
- 填充长度与剩余百分比一致。
- 填充颜色根据主题变化，并允许从主色自然提亮形成轻微渐变。
- 不允许秋季进度条末端混入绿色，也不允许危急主题仍使用绿色。

## 8. 额度主题系统

主题以`剩余百分比`为唯一判断依据，边界必须严格一致。

| 剩余额度 | 主题 | 背景方向 | 进度条颜色 |
|---:|---|---|---|
| 50%–100% | 森林 | 深绿色树林、林间光线、自然丁达尔效果 | 薄荷绿 |
| 20%–49% | 秋天 | 金棕色树林、枫叶、温暖斜射光 | 枫叶金黄 |
| 3%–19% | 末日 | 暗红天空、压迫感、远处毁坏环境 | 警示红 |
| 0%–2% | 废土 | 灰白废墟、断壁残垣、枯萎植物、萧败感 | 警示红或灰红 |

边界测试要求：

- 50% 必须显示森林主题。
- 49% 必须显示秋季主题。
- 20% 必须显示秋季主题。
- 19% 必须显示末日主题。
- 3% 必须显示末日主题。
- 2%、1% 和 0% 必须显示废土主题。

## 9. 产品图标

- 使用产品自有图标，不直接复制 Codex 应用图标。
- 图形方向建议为`命令提示符 + 叶片或圆环进度`，表达 AI 工具与自然额度主题的结合。
- 紧凑卡片内使用白色或接近白色版本。
- 图标放在`7 天额度`文字之后，与标题基线视觉对齐。
- 图标在 14–16 pt 尺寸下仍应清晰，不使用过多细节。

## 10. 交互需求

### 10.1 点击与展开

- 单击卡片：在收起与展开状态之间切换。
- 展开和收起时，宽度不得变化。
- 高度变化使用 160–220 ms 的平滑动画。
- 双击卡片：立即请求刷新额度数据。
- 单击与双击必须正确区分，双击不得误触发展开后再收起。

### 10.2 拖动

- 用户可按住卡片任意非控制区域拖动。
- 卡片必须紧跟鼠标，不得出现明显延迟、跳跃或吸附阻力。
- 拖动期间不执行尺寸动画。
- 拖动结束后保存位置，下次启动恢复。
- 目标体验：在 60 Hz 屏幕上感知不到明显卡顿。

### 10.3 侧边收纳

- 仅支持屏幕左边缘和右边缘。
- 只有卡片真正贴到当前显示器可用区域边缘时才触发收纳。
- 未完全贴边时不得提前吸附或隐藏。
- 收纳后隐藏完整卡片，只保留一条竖向进度条。
- 竖向进度条长度和颜色反映当前剩余额度。
- 鼠标移入竖向进度条区域时，完整卡片平滑滑出。
- 鼠标离开后，在短暂延迟后重新收纳，避免误操作。
- 用户将展开后的卡片拖离边缘时，退出侧边收纳状态。

### 10.4 多显示器

- 拖动、贴边和恢复位置都以卡片当前所在显示器为准。
- 不允许卡片启动后落在不可见区域。
- 显示器断开时，将卡片移动到主显示器的安全区域。

## 11. 数据需求

### 11.1 数据字段

统一数据模型至少包含：

- 额度窗口类型，例如 7 天或其他实际返回窗口。
- 剩余百分比。
- 已用百分比；如数据源不提供，可由可信字段计算。
- 重置时间。
- 数据获取时间。
- 数据来源类型。
- 套餐名称；仅在可靠获得时使用。

### 11.2 数据获取原则

- 优先研究并使用官方公开接口或本机可合法调用的接口。
- 如果没有稳定公开接口，可研究用户本机能够合法读取的本地输出，但必须独立编写数据适配层。
- 不复制其他项目的数据解析代码。
- 不读取与额度无关的对话正文或用户内容。
- 不将任何本地数据上传到第三方服务器。
- 数据源调查结果应单独记录在`data-source-notes.md`中。

### 11.3 刷新策略

- 正常刷新间隔建议为 10–30 秒，最终数值由性能测试确定。
- 支持用户双击手动刷新。
- 避免每次刷新扫描大量历史文件。
- 数据源不可用时保留最近一次可信快照，并明确显示数据已过期。
- 如果从未成功获得数据，显示`--%`和`等待额度数据`。

### 11.4 Codex 运行状态

- Codex 未运行时，默认隐藏悬浮卡片。
- Codex 启动后，卡片自动出现并开始刷新。
- Codex 关闭后，卡片自动隐藏，但保留用户位置和展开状态。

## 12. 系统行为

### 12.1 窗口行为

- 窗口浮于普通应用窗口之上，但不抢夺键盘焦点。
- 不出现在 Dock。
- 不占用菜单栏位置。
- 可在不同桌面空间中保持可见。
- 全屏应用中的显示行为应可预测，不遮挡关键系统控件。

### 12.2 启动与卸载

- 提供明确的一键安装脚本或标准应用安装方式。
- 支持用户登录后自动启动。
- 提供完整卸载方式，删除自动启动配置和安装文件。
- 不修改系统安全设置。
- 不要求管理员权限，除非未来采用的安装方式确有必要并向用户解释原因。

## 13. 隐私与安全

- 所有处理在本机完成。
- 不收集遥测数据。
- 不记录对话正文。
- 不保存 GitHub、OpenAI 或其他账号凭据。
- 日志默认只记录运行状态和错误类型，不记录用户内容和敏感路径。
- README 必须明确说明这是非官方工具，与 OpenAI 无隶属或背书关系。
- 内部或非公开接口发生变化时，应安全失败，不得尝试绕过访问控制。

## 14. 性能要求

- 空闲状态平均 CPU 占用目标低于 1%。
- 常驻内存目标低于 80 MB。
- 卡片拖动应接近屏幕刷新率，无明显掉帧。
- 正常刷新不得造成界面卡顿。
- 首次启动到卡片可用目标不超过 1.5 秒，不含外部数据源等待时间。
- 背景图片只在需要时解码，并避免每次刷新重复加载。

## 15. 无障碍要求

- 百分比必须以文字展示，不能只依赖颜色。
- 主题变化不能作为唯一的额度告警方式。
- 标题、百分比、重置时间和进度条提供 VoiceOver 标签。
- 尊重系统`减少动态效果`设置；开启时使用淡入淡出或直接切换。
- 文字与背景保持足够可读性。
- 右键菜单或设置入口提供键盘可访问方案。

## 16. 异常状态

| 场景 | 产品行为 |
|---|---|
| 第一次启动且无额度数据 | 显示等待状态，不显示虚构百分比 |
| 数据暂时读取失败 | 保留最近可信值，并标记数据陈旧 |
| 数据格式发生变化 | 安全失败、记录非敏感错误，不崩溃 |
| 剩余百分比小于 0 或大于 100 | 拒绝异常值或限制显示范围，并记录错误 |
| 重置时间缺失 | 显示`重置时间未知` |
| 背景素材缺失 | 使用对应主题的纯色或渐变备用背景 |
| 保存的位置超出屏幕 | 自动恢复到主显示器安全区域 |
| 自动启动配置失败 | 工具仍可手动运行，并给出明确修复提示 |

## 17. 验收标准

### 17.1 核心功能

- [ ] 能在本机获取并展示真实的 7 天剩余额度。
- [ ] 收起卡片为 200 × 80 pt。
- [ ] 展开时宽度保持 200 pt，只增加高度。
- [ ] 单击展开、双击刷新互不冲突。
- [ ] 卡片可以顺滑拖动并记住位置。
- [ ] 只有完全贴边后才进入侧边收纳。
- [ ] 收纳后只显示竖向进度条。
- [ ] Codex 未运行时卡片隐藏，运行后自动出现。

### 17.2 主题

- [ ] 50%、49%、20%、19%、3%、2%、1%、0% 均通过边界测试。
- [ ] 秋季主题进度条为枫叶金黄，不出现绿色末端。
- [ ] 末日和废土主题具有明显但不过度刺眼的警示感。
- [ ] 所有主题上的文字清晰可读。

### 17.3 视觉

- [ ] 圆角为 18 pt，边框为 1 pt、80% 不透明度。
- [ ] 卡片轮廓在浅色、深色和复杂壁纸上均可辨认。
- [ ] Retina 截图中边缘平滑、没有明显锯齿。
- [ ] 背景不拉伸，卡片比例始终正确。
- [ ] 图标位于标题之后，并与标题视觉对齐。

### 17.4 稳定性与隐私

- [ ] 连续运行 8 小时无崩溃、无持续内存增长。
- [ ] 数据源中断和恢复时界面状态正确。
- [ ] 多显示器连接、断开后卡片仍可见。
- [ ] 网络断开不影响本地已有功能。
- [ ] 未发现额度信息或用户内容被上传。

## 18. 开发阶段建议

### Phase 1：独立数据验证

- 调查可用数据源。
- 写出数据源说明和隐私边界。
- 用命令行程序输出标准化额度快照。
- 验证异常值、缺失值和数据源变化。

### Phase 2：静态卡片

- 创建悬浮窗口。
- 实现 200 × 80 的收起布局。
- 使用模拟数据完成信息层级和视觉验收。

### Phase 3：交互

- 实现展开、收起和宽度锁定。
- 实现顺滑拖动、位置保存和多显示器约束。
- 实现完全贴边后侧边收纳。

### Phase 4：主题

- 接入四套有明确权利来源的背景素材。
- 完成主题阈值、进度条颜色和边界测试。

### Phase 5：系统集成

- 接入真实额度数据。
- 实现 Codex 运行状态检测。
- 实现自动启动、安装与卸载。

### Phase 6：发布前验收

- 构建 Release 版本。
- 完成性能、隐私、多显示器和长时间运行测试。
- 更新 README、截图、许可证和第三方声明。
- 从一台未安装开发环境的测试 Mac 验证安装流程。

## 19. 发布文案建议

完成独立实现并确认没有复用第三方实质性代码后，可以使用：

> Quota Grove 是由 Ayang 主导设计与独立开发的 macOS Codex 桌面额度工具。产品使用 AI 辅助完成工程实现，并由 Ayang 负责需求、视觉设计、功能决策、测试与最终验收。

不要使用：

> 全球首创、完全没有参考任何同类产品、全部代码均由本人逐行手写。

## 附录 A：新 Codex 任务启动提示词

将以下内容连同本 PRD 一起交给一个新的 Codex 任务：

```text
请在一个全新的空目录中，从零开发 PRD 描述的 macOS 应用 Quota Grove。

这是独立实现任务。你只能使用：
1. Quota-Grove-PRD.md；
2. 我明确提供且拥有使用权的图片和视觉参考；
3. Apple、OpenAI 等官方公开文档；
4. 本机合法可观察的运行状态和数据输出；
5. 许可兼容的通用依赖。

禁止读取、搜索、复制或参考 wendybzhang/codex-quota-widget 及其任何 Fork 的源代码、README、脚本、文件结构、类型名称、函数名称或 Git 历史。不要对既有项目做变量改名、逐行重写或结构翻译。

开发要求：
- 创建全新的 Git 仓库，从第一次架构提交开始保留历史；
- 先完成 requirements-review.md、architecture.md 和 data-source-notes.md，再编写正式功能代码；
- 数据源必须独立调查和实现，并记录来源、隐私边界与失败策略；
- 每个阶段都需要真实构建、运行和验证，不能只生成代码；
- 使用模拟额度测试 50、49、20、19、3、2、1、0 八个主题边界；
- 验证拖动、宽度锁定、侧边收纳、多显示器、自动启动和卸载；
- 不上传任何本地额度、日志或用户内容；
- 不使用 Codex 官方图标作为产品主图标，创建具有独立识别度且权利清晰的图标；
- 最终交付源码、安装方式、卸载方式、README、测试报告和可运行构建。

不要访问旧项目路径。如果你发现完成任务必须读取旧项目，请停止并向我说明原因，不要自行突破这一边界。
```

## 附录 B：交付物清单

- [ ] 全新的 Git 仓库和完整提交历史。
- [ ] 产品源代码。
- [ ] `requirements-review.md`。
- [ ] `architecture.md`。
- [ ] `data-source-notes.md`。
- [ ] `PRIVACY.md`。
- [ ] `THIRD_PARTY_NOTICES.md`。
- [ ] 安装和卸载说明。
- [ ] 主题背景与图标来源记录。
- [ ] 功能与边界测试报告。
- [ ] 性能和长时间运行报告。
- [ ] 发布截图和 README。
