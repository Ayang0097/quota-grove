# Quota Grove 需求复核

日期：2026-08-27

## 交付目标

从空目录独立实现一个不显示 Dock 图标、不占菜单栏的原生 macOS 桌面卡片。卡片读取本机 Codex 运行事件中的 7 天额度，默认展示剩余百分比、重置倒计时与主题化进度，并支持展开、拖动、贴边收纳、登录启动和异常回退。

## V1 决策

- 技术形态：Swift 6 + AppKit，无第三方运行时依赖。
- 窗口：`NSPanel` 非激活浮窗，收起 `200 × 80 pt`，展开 `200 × 178 pt`。
- 数据：只读扫描 `~/.codex/sessions/**/*.jsonl` 最近变更文件，仅解析带 `rate_limits` 的事件行。
- 7 天窗口：优先选择 `window_minutes == 10080`；不存在时不猜测为 7 天，而显示实际窗口。
- 百分比：`remaining = 100 - used_percent`，异常值拒绝，不进入界面。
- 刷新：20 秒轮询；双击立即重新扫描。没有公开刷新接口，因此双击不向 OpenAI 发请求。
- Codex 状态：通过 macOS 运行应用列表判断 ChatGPT/Codex 客户端是否在运行；未运行时隐藏。
- 主题：正式背景使用产品负责人提供的四张 2.5:1 图片；Core Graphics 原生场景作为缺图回退。
- 自动启动：使用用户级 LaunchAgent，不要求管理员权限。
- 发布：安装到 `~/Applications/Quota Grove.app`，默认不自动启用登录启动。

## 验收映射

| PRD 要求 | 实现/验证方式 |
|---|---|
| 真实 7 天额度 | JSONL 适配器 + `--snapshot-json` 实机命令 |
| 四个主题边界 | XCTest 覆盖 50/49/20/19/3/2/1/0 |
| 单击/双击互斥 | 延迟单击，收到第二击时取消 |
| 展开宽度不变 | 窗口控制器只改高度并保留顶部坐标 |
| 拖动与位置保存 | 自定义鼠标拖动 + UserDefaults |
| 完全贴边收纳 | 仅当窗口经约束后触及当前屏幕可用区域边界时触发 |
| 多显示器安全 | 按最大相交面积选择当前屏幕；屏幕变化时重新约束 |
| 登录启动/卸载 | `install.sh`、右键登录启动开关、`uninstall.sh` |
| 隐私 | 无网络代码、无遥测、无凭据读取 |

## 已知边界

本机 JSONL `rate_limits` 事件不是官方公开稳定 API。内部字段或存储位置改变时，应用会保留最近可信值，并在展开态显示数据更新时间；从未取得过数据时显示 `--%`。V1 不尝试绕过访问控制，也不调用未公开的远程接口。

在没有活动 Codex 任务时，本地事件可能暂时不更新。此时剩余百分比仍使用最近可信快照，用户可在展开态查看数据更新时间。
